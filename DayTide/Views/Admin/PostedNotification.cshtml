@model IEnumerable<DayTide.Models.Notice>

@{
    ViewBag.Title = "PostedNotification";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Posted Notification By @Session["UserID"]</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Massage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.Password)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Massage)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Admin.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Password)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>*@
<div class="row">
    @foreach (var item in Model)
    {

        <div class="col-sm-6">
            <div class="card">
                <div class="card-body">
                    <h4>Massage</h4>

                    <p class="card-text"> @Html.DisplayFor(modelItem => item.Massage)</p>

                    <h5>Sent For :</h5>

                    @Html.DisplayFor(modelItem => item.Send_For)<br />
                    @Html.DisplayFor(modelItem => item.User.Type)<br />
                    <a href="/Admin/EditNotice/@item.Id" class="btn btn-primary">Edit</a>
                    <a href="/Admin/DeleteNotice/@item.Id" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>

    }
</div>
@Html.ActionLink("Back To Home", "Index")