@model IEnumerable<DayTide.Models.Notice>

@{
    ViewBag.Title = "Mynotification";
    Layout = "~/Views/Shared/_Layout_Admin.cshtml";
}

<h2>Mynotification</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
@*<table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Massage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Admin.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.User.Password)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Massage)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Admin.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Password)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }


    </table>*@

<div class="row">
    @foreach (var item in Model)
    {
        if (item.Status == "Unread")
        {
            <div class="col-sm-6">
                <div class="card">
                    <div class="card-body" style="background-color:rgba(0,0,255,0.1);">
                        <h4>A  New Massage From @item.Send_by <b>@item.User.Type</b></h4>

                        @* <p class="card-text"> @Html.DisplayFor(modelItem => item.Massage)</p>

                            <h5>Sent By :</h5>

                            @Html.DisplayFor(modelItem => item.Send_by)<br />
                            @Html.DisplayFor(modelItem => item.User.Type)*@
                        <a href="/Admin/viewFullMassege/@item.Id" class="btn btn-primary">Detail</a>
                    </div>
                </div>
            </div>

        }
        if (item.Status == "read")
        {
            <div class="col-sm-6">
                <div class="card">
                    <div class="card-body" >
                        <h4>A  New Massage From @item.Send_by <b>@item.User.Type</b></h4>

                        @* <p class="card-text"> @Html.DisplayFor(modelItem => item.Massage)</p>

                            <h5>Sent By :</h5>

                            @Html.DisplayFor(modelItem => item.Send_by)<br />
                            @Html.DisplayFor(modelItem => item.User.Type)*@
                        <a href="/Admin/viewFullMassege/@item.Id" class="btn btn-primary">Detail</a>
                    </div>
                </div>
            </div>


        }



    }
</div>
@Html.ActionLink("Back To Home", "Index") 